services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ..:/workspace:cached
      - node_modules:/workspace/node_modules
      - backend_node_modules:/workspace/apps/backend/node_modules
      - frontend_node_modules:/workspace/apps/frontend/node_modules
      - package_config_node_modules:/workspace/packages/config/node_modules
      - package_constants_node_modules:/workspace/packages/constants/node_modules
      - package_types_node_modules:/workspace/packages/types/node_modules
      - package_utils_node_modules:/workspace/packages/utils/node_modules
    command: sleep infinity
    networks:
      - devnet
    depends_on:
      - db
      - redis

  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: app_dev
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - '5532:5432'
    networks:
      - devnet

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: redis-server --requirepass redis_password
    volumes:
      - redis-data:/data
    ports:
      - '6479:6379'
    networks:
      - devnet

networks:
  devnet:
    driver: bridge

volumes:
  postgres-data:
  redis-data:
  node_modules:
  backend_node_modules:
  frontend_node_modules:
  package_config_node_modules:
  package_constants_node_modules:
  package_types_node_modules:
  package_utils_node_modules:
